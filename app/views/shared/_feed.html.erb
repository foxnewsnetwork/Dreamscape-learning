<% unless @feed_items.empty? %>
  <% @feed_items.each do |feed_item| %>
    <table class="macroposts" summary="User macroposts">
      <%= render :partial => 'shared/feed_item' , :object => feed_item %>
    </table>
  <% end %>
  <%= will_paginate @feed_items %>
<% end %>
